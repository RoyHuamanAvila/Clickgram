import './Header.scss';

const Header = () => {
	return (
		<header className='header'>
			<img className='header-logo' src="/Logo.svg" alt="Header logo" />
		</header>
	);
};

export default Header;
